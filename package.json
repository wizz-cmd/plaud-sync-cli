{
  "name": "plaud-sync-cli",
  "version": "1.0.0",
  "description": "Standalone CLI tool to sync Plaud.ai recordings to local markdown files",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "plaud-sync": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "keywords": ["plaud", "sync", "cli", "markdown", "transcription"],
  "author": "Leonard Sellem (original), replatformed as CLI",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
